# inputtest.cfg
# Simple ProDOS BRUN-style binary loaded at $2000.

MEMORY {
    MAIN: start = $2000, size = $6000, file = %O;
}

SEGMENTS {
    CODE: load = MAIN, type = ro;
    DATA: load = MAIN, type = rw;
}
